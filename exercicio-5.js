let operacao = prompt("negativo ou positivo")

/*
if(operacao < 0)
{
    alert(`negativo`) 
} else{
    alert(`positivo`)
} */

//if ternário
alert(`o numero ${operacao} é ${(operacao < 0) ? 'negativo' : 'positivo'}`)